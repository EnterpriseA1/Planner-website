{
  "compilerOptions": {
    /* Target & module (Bun รัน ESM ได้เต็ม) */
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",

    /* Bun runtime types + ESM interop */
    "types": ["bun-types"],
    "esModuleInterop": true,
    "resolveJsonModule": true,

    /* ความเข้มงวด */
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    /* Project structure */
    "rootDir": "src",
    "outDir": "dist",

    /* ไม่ต้อง JSX/JS ใน backend */
    "allowJs": false,
    "jsx": "preserve",

    /* ให้ Bun/ts-node style แปลงเองระหว่าง dev */
    "noEmit": true,

    /* ตัวเลือกที่ทำให้ import ชัดเจนขึ้น */
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": false,

    /* ทางเลือกถ้าจะใช้ alias ภายหลัง */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}